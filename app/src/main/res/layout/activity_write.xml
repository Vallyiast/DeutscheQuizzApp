<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:gravity="center_horizontal"
    android:padding="24dp"
    android:background="@drawable/background_strips_grad"
    tools:context=".WriteActivity">

    <!-- Bouton menu (coin supérieur) -->
    <Button
        android:id="@+id/main_menu"
        android:text="Menu"
        style="@style/ButtonMenu"
        android:layout_marginBottom="24dp"/>

    <!-- Carte de la question -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:backgroundTint="#b1894e"
        android:padding="16dp"
        android:elevation="6dp"
        android:layout_marginBottom="32dp"
        android:layout_marginHorizontal="16dp"
        android:gravity="center">

        <TextView
            android:id="@+id/question"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Question"
            android:textColor="@android:color/black"
            android:textSize="22sp"
            android:textStyle="bold"
            android:textAlignment="center"
            android:padding="8dp" />

        <TextView
            android:id="@+id/reponse"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Réponse"
            android:textAlignment="center"
            android:textColor="#ffffff"
            android:padding="12dp"
            android:textSize="20sp"
            android:background="@drawable/rounded"
            android:backgroundTint="#b1894e"
            android:layout_marginTop="12dp" />

    </LinearLayout>

    <!-- Boutons d’action -->
    <LinearLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginBottom="24dp"
        android:orientation="horizontal"
        android:gravity="center">

        <Button
            android:id="@+id/buttonSubmit"
            style="@style/ButtonCommon"
            android:text="Envoyer"
            android:layout_marginHorizontal="8dp"/>

        <Button
            android:id="@+id/traduction"
            style="@style/ButtonCommon"
            android:text="Traduction"
            android:layout_marginHorizontal="8dp"/>
    </LinearLayout>


    <Button
        android:id="@+id/next_button"
        style="@style/ButtonCommon"
        android:text="Suivant"
        android:layout_marginBottom="32dp"/>

    <!-- Ligne de boutons lettres -->
    <LinearLayout
        android:id="@+id/lettres_container"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center"
        android:padding="12dp"
        android:background="@drawable/rounded"
        android:backgroundTint="#b1894e"
        android:layout_marginTop="16dp"
        android:layout_marginBottom="24dp"
        android:elevation="4dp">

        <!-- Exemple : ces boutons peuvent être générés dynamiquement -->
        <Button
            android:id="@+id/lettre1"
            style="@style/ButtonCharacter"
            android:text="A"/>

        <Button
            android:id="@+id/lettre2"
            style="@style/ButtonCharacter"
            android:text="B"/>

        <Button
            android:id="@+id/lettre3"
            style="@style/ButtonCharacter"
            android:text="C"/>

        <Button
            android:id="@+id/lettre4"
            style="@style/ButtonCharacter"
            android:text="D"/>

        <Button
            android:id="@+id/lettre5"
            style="@style/ButtonCharacter"
            android:text="E"/>
        <Button
            android:id="@+id/lettre6"
            style="@style/ButtonCharacter"
            android:text="F"/>
    </LinearLayout>
</LinearLayout>
